# Backlog

This is a lightweight list of possible future work, not a release commitment.

## Next

- Add fixture-based tests for the generic extraction fallback and user site profiles.

## Later

- Add maintained default JSON profiles for well-known publishing platforms and sites. Start with Medium and Substack web pages; only ship profiles backed by fixtures and extraction tests.
- Add `offprint serve`: run a local web application for managing archived articles and posts, editing global settings, previewing PDF configuration, visually selecting crawled articles, and turning the selection into a bundle.
- Add a `Dockerfile` and `docker-compose.yml` for running `offprint serve` after the server workflow exists.
